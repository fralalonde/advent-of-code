use std::io;
use std::fs::File;
use std::io::{BufReader, BufRead};
use anyhow::*;

fn main() -> Result<()> {

    println!("min dist");

    Ok(())
}

#[cfg(test)]
mod test {

    #[test]
    fn bop() {
    }
}
